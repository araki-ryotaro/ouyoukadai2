<% if users.exists? %>
  <% users.each do |user| %>
  <table class='table'>
    <tr>
      <th>name</th>
      <th></th>
      <th></th>
    </tr>
    <tr>
      <td><%= user.name %></td>
      <td>フォロー数: <%= user.followings.count %></td>
      <td>フォロワー数: <%= user.followers.count %></td>
    </tr>
  </table>
  <% end %>
<% else %>
  <p>ユーザーはいません</p>
<% end %>
